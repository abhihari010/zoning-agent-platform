[pytest]
# Scoped pytest config for the WS1 scraper. Lets
#   python -m pytest services/ingestion/scraper/tests -q
# be run from the repo root without disturbing apps/api's own test config.
testpaths = tests
python_files = test_*.py
addopts = -q
